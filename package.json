{
  "name": "universal-music-theory-library",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:umt": "npx -y esbuild lib/music-theory/umt.ts --bundle --minify --sourcemap --format=iife --global-name=UMT --outfile=docs/umt.js",
    "lint": "eslint .",
    "test:unit": "vitest run"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^20",
    "eslint": "9.39.1",
    "typescript": "5.9.3",
    "vitest": "^4.1.4"
  }
}
